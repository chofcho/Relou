version: '2'
services:
    mstest-mongodb:
        image: mongo:3.4.8
        ports:
            - "27017:27017"
        command: mongos --configdb mstest-mongodb-config
    mstest-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    mstest-mongodb-config:
        image: mongo:3.4.8
        container_name: mstest-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
